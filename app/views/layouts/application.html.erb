<!DOCTYPE html>
<html>
  <head>
    <title>Llm Memory</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if authenticated? %>
      <!-- Navigation Header -->
      <nav class="bg-white/90 backdrop-blur-sm border-b border-white/20 shadow-sm">
        <div class="max-w-7xl mx-auto px-6">
          <div class="flex items-center justify-between h-16">
            <div class="flex items-center space-x-8">
              <%= link_to dashboard_path, class: "text-xl font-bold text-neutral-900 font-ai" do %>
                Llm Memory
              <% end %>
              
              <div class="hidden md:flex items-center space-x-6">
                <%= link_to dashboard_path, 
                    class: "text-neutral-600 hover:text-neutral-900 px-3 py-2 rounded-lg text-sm font-medium transition-colors duration-200" do %>
                  Dashboard
                <% end %>
                
                <%= link_to chats_path, 
                    class: "text-neutral-600 hover:text-neutral-900 px-3 py-2 rounded-lg text-sm font-medium transition-colors duration-200" do %>
                  AI Chat
                <% end %>
              </div>
            </div>
            
            <div class="flex items-center space-x-4">
              <span class="text-sm text-neutral-600">
                <%= Current.user.email_address %>
              </span>
              <%= link_to session_path, method: :delete, 
                  class: "text-neutral-600 hover:text-neutral-900 px-3 py-2 rounded-lg text-sm font-medium transition-colors duration-200" do %>
                Sign Out
              <% end %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <%= yield %>
  </body>
</html>
